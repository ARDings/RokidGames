<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Bewusst klassisches Black.NoTitleBar.Fullscreen statt Material.
      Material zieht auf Android Go (Sprite) AppCompat-Abhängigkeiten und Animations
      mit, die wir hier nicht brauchen und die teilweise instabil rendern.
    -->
    <style name="Theme.HeadPong" parent="android:Theme.Black.NoTitleBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:colorBackground">@android:color/black</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
